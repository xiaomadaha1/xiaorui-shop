generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model User {
  USERNAME String @id @unique
  PWD      String
}

model Goods {
  ID    String @id @unique
  NAME  String
  Price Float
}
